Group by : used to generate summary report on the given tables.

DDL: data defination lang
if we want to modify, delete,create we use ddl
command: create,alter,drop
trauncate:clears everything but table structure will be there
(ddl command cannot be roll back,once we commit that cannot be rollback)


DML: data manipulation lang(preform manupliction)
Insert 
Uptate
Delete: database will have log file which has entires so we can roll back

datatype: int float double date char varchar enum
varchar: redundancy is less in varchar
varchar(max)